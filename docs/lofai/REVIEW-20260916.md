# moe-cache 分支審查報告（2026-09-16，基準 commit 925933801）

審查方式：相對上游 ggml-org/llama.cpp 分歧點 82d6bb284 的 diff（158 檔、+47K 行），分七片由獨立審查者各看一片，主要發現逐條抽樣核對原始碼。
部署情境：RTX 5070 Ti 16GB + 96GB RAM、Qwen3.8-Flash-Next、`-np 1`、全量釘住（未設 `--moe-expert-cache-host-pinned-mb`）、MTP 草稿頭、`--decode-overlap --backend-sampling`、CUDA Graph。

## 總結
- 🔴 0 條。沒有發現在現行配置下會直接算錯或損毀資料的問題。
- 🟡 7 條，多數是「現行配置碰不到、換配置就會爆」的延遲雷，其中一條（裝置端無逾時忙等）可能讓整台 GPU 無聲卡死，上生產前要處理。
- 整體工程品質高於一般 fork：溢位檢查、RAII、CUDA Graph 指紋校驗、狀態機斷言都做得紮實。

## 處理狀態（2026-09-16 更新）

| # | 狀態 | commit / 待辦 |
|---|---|---|
| 1 | ✅ 已修＋回歸測試 | `5e5bc5c67` 4 秒逾時 + host-mapped 故障旗標，graph_compute 回 `GGML_STATUS_FAILED`；測試 `test-moe-cache --mmq-stage-wait-only`（修前 20 秒仍卡住→FAIL，修後 4.2 秒通過，2026-09-16 主機 A 實測） |
| 2 | ✅ 已修（防禦性） | `66db628e4` 三處相減改先算已用量，補不變量 ASSERT。**追查後確認現行呼叫順序下無法觸發**：staging 預留在 configure 時對 limit 檢查、來源登記與 staging 配置各自檢查，欄位總和不可能超過 limit；屬硬化不是可重現 bug，不符合上游「bug-fix PR 需可重現」門檻，不提 PR |
| 6 | ✅ 已修 | `35e2b608e` 新增 `prefetch_failed` 統計欄位 |
| 3 | 📝 待辦 | 現為死碼；日後若為 qwen4exp 補 sparse snapshot override 時**一起修**呼叫端回傳值檢查 |
| 4 | 📝 待辦 | 邊緣；等有 decode-overlap 失敗實例再修（throw 前清三欄位） |
| 5 | 📝 待辦 | 只在 `-np>1` 觸發；我們固定 `-np 1`，若改多 slot 前必修 |
| 7 | 📝 待辦 | 單卡不會碰；加第二張卡前必修（device scope） |

上生產前的門檻：#1 #2 #6 已修並回歸測試（見 Obsidian 知識/Qwen3.8-Flash-Next 實測）；#3/#5/#7 各綁一個觸發條件，條件出現前不動。

## 🟡 清單（依處理優先序）

| # | 位置 | 問題 | 現行配置會碰到？ | 修法 |
|---|---|---|---|---|
| 1 | `ggml/src/ggml-cuda/mmq.cuh:1057,1171,1275` | GPU kernel 內 `while (*ready == 0) {}` 無逾時忙等，等 copy stream 寫旗標；生產者出狀況即整台 GPU 卡死、無日誌 | **會**（MoE cache 專屬路徑 `use_x_map`） | 有界自旋 + 逾時後 `__trap()` 或設錯誤旗標讓 host 中止；host 端 `ggml-cuda.cu:2634` 已有 1ms 逾時版本可當範本 |
| 2 | `ggml/src/ggml-cuda/moe-cache-host.cu:505` 與 `:136` | `limit − staging_reserved − source_bytes` 直接相減，size_t 下溢讓預算牆失效，可超額釘住 | 否（只在設 `--moe-expert-cache-host-pinned-mb` 時） | 先驗證 `staging + source ≤ limit` 不變量再比較；錯誤訊息同源修正 |
| 3 | `src/llama-memory-recurrent.cpp:216` + `common/speculative.cpp:771,1735,1845` | sparse rollback 找不到 checkpoint 靜默回 false，三處呼叫端不檢查回傳值 | 否（qwen4exp 未開 sparse snapshot，死碼） | 呼叫端檢查回傳值；若日後為 qwen4exp 補 override 必須一起修 |
| 4 | `tools/server/server-context.cpp:4384-4392` | decode-overlap 排入失敗只回滾 KV/sampler，`decode_overlap_pos` 等三欄位沒清 | 邊緣（目前失敗會 abort 全部 slot，不會靜默） | throw 前清空三欄位 |
| 5 | `tools/server/server-context.cpp:3297-3305` | replay 恢復失敗 `return` 整個 update_slots，`-np>1` 時餓死其他 slot | 否（`-np 1`） | 改 continue 或註明限制 |
| 6 | `ggml/src/ggml-cuda/moe-cache.cu:7677` | prefetch 回傳碼 `(void)` 丟棄，cache thrashing 無訊號 | 會（可觀測性） | 累計進既有 `legacy_op_stats` |
| 7 | `ggml/src/ggml-cuda/moe-cache.cu:4374-4386` | `early_workspace` 解構子缺裝置切換 | 否（單卡） | 比照 `grouped_device_resource` 加 device scope |

## 🟢 備註
- `moe-cache.cu:10349` barrier 等待無逾時（RAII 保證下安全，建議加診斷 log）。
- `staged-input.cu:38` 硬編 1MB 上限無註解，超過即靜默退回 host input。
- `moe-cache.cu:2562` `n_routes==0` 除零僅靠呼叫端保證。
- `moe-cache.cu:8153-8154` prepack callback 鏈尾傳空請求，推測性風險，未證實。
- `mmid.cuh` 的 `physical_id_upper_bound` 實際用在 `ggml-cuda.cu:1994`，本輪未逐行驗證。

## 已驗證乾淨的區域
釘住失敗回滾與 refcount 解構順序、worker thread 同步、LRU 不會 evict 被 pin 的槽、CUDA Graph capture/replay 指紋校驗與回退、staged-input 雙緩衝自我探測、前綴快取與 MTP 狀態邊界、sampler 快照順序、旗標互斥檢查、PLE lazy 讀取錯誤傳播、緊湊因果遮罩拒絕鏈、KV 量化探測、allocator 共用結構的錯誤傳遞、`GGML_CUDA_MOE_EARLY_ROUTER` 預設關閉。
